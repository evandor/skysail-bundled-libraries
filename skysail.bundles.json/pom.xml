<?xml version="1.0"?>
<project>

	<parent>
		<groupId>de.twentyeleven.skysail</groupId>
		<artifactId>skysail.build.maven.parentPom</artifactId>
		<version>0.7.11</version>
	</parent>
	
	<properties>
		<bundledGroup>org.json</bundledGroup>
		<bundledArtifact>json</bundledArtifact>
		<bundledVersion>20070829</bundledVersion>
	</properties>

	<modelVersion>4.0.0</modelVersion>
	<groupId>de.twentyeleven.bundled</groupId>
	<artifactId>${bundledArtifact}</artifactId>
	<packaging>bundle</packaging>
	<name>OSGi ${bundledArtifact} classes</name>
	<version>${bundledVersion}</version>
	
	<scm>
		<!-- read access -->
		<connection>scm:git:git@github.com:evandor/skysail-bundled-libraries.git</connection>
		<!-- write access -->
		<developerConnection>scm:git:git@github.com:evandor/skysail-bundled-libraries.git</developerConnection>
		<!-- A publicly browsable repository. For example, via ViewCVS. -->
		<url>scm:git:git@github.com:evandor/skysail-bundled-libraries.git</url>
	</scm>
	

	<dependencies>
		<dependency>
			<groupId>${bundledGroup}</groupId>
			<artifactId>${bundledArtifact}</artifactId>
			<version>${bundledVersion}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<extensions>true</extensions>
				<configuration>
					<instructions>
						<_include>-osgi.bnd</_include>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>
	
</project>