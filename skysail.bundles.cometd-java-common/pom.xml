<?xml version="1.0"?>
<project>
	<parent>
		<groupId>de.twenty11.skysail</groupId>
		<artifactId>skysail.build.maven.parentPom</artifactId>
		<version>0.1.0</version>
	</parent>
	
	<properties>
		<group>org.cometd.java</group>
		<artifact>cometd-java-common</artifact>
		<version>2.4.0.RC2</version>
	</properties>

	<modelVersion>4.0.0</modelVersion>
	<groupId>${group}</groupId>
	<artifactId>skysail.bundles.${artifact}</artifactId>
	<packaging>bundle</packaging>
	<name>OSGi ${artifact} classes</name>
	<version>${version}</version>

	<dependencies>
		<dependency>
			<groupId>${group}</groupId>
			<artifactId>${artifact}</artifactId>
			<version>${version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<extensions>true</extensions>
				<version>2.3.5</version>
				<configuration>
					<instructions>
						<_include>-osgi.bnd</_include>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>